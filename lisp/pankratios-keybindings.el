;;; pankratios-keybindings --- global key bindings

;;; Commentary:
;; Global key mapping changes

;;; Code:

(global-set-key (kbd "M-RET") 'duplicate-line)

(provide 'pankratios-keybindings)
;;; pankratios-keybindings.el ends here
